from finki_map_mis.widgets.py.SurfaceWidget import SurfaceWidget


class FloorWidget(SurfaceWidget):
    """
    A Surface with possible elevation where there is a different
    layout of children (Rooms) on each elevation level
    """