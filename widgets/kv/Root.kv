<Root>:
    orientation: "vertical"

    BoxLayout:
        MapRootWidget:
            id: map_root_widget

    MDTopAppBar:
        id: top_app_bar
        title: "ФИНКИ кампус (спрат 0)"
        md_bg_color: "#286090"
        # elevation: 0
        pos_hint: {"top": 1}
        right_action_items: [["stairs", lambda x: app.open_floor_menu(x)]]
